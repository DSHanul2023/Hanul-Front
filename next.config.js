/** @type {import('next').NextConfig} 
const nextConfig = {}

module.exports = {
 config options here 
}*/

const nextConfig = {
  images: {
    domains: ['image.tmdb.org'],
  },
};

module.exports = nextConfig;

